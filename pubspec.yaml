name: arkadasai_mvp
description: A cloud-only Flutter MVP for ArkadaşAI.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  http: ^1.2.0
  flutter_secure_storage: ^9.0.0
  intl: ^0.18.0
  # foundation is part of Flutter; no extra package required

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/i18n/
    - assets/lottie/